[project]
name = "pepfold4-service"
version = "1.0.0"
description = "PEP-FOLD4 微服务 - 肽从头结构预测 (Docker 封装)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
]
