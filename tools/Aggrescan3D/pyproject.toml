[project]
name = "aggrescan3d-service"
version = "1.0.0"
description = "Aggrescan3D 微服务 - PDB 结构聚集倾向分析 (Docker 封装)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
]
