"""
流水线编排 — 融合蛋白设计评估流程。

本文件是整个项目的"主程序"，``python -m main`` 会执行 ``run()`` 函数。

当前为重置后的空壳，待新版 Pipeline 设计实现。
"""

from __future__ import annotations


async def run() -> None:
    """Pipeline 入口，由 ``main/__init__.py`` 通过 asyncio.run() 调用。"""
    raise NotImplementedError("Pipeline is being redesigned.")
